# Maister Builder — Python dependencies
# Tested on Python 3.13. Install with:  pip install -r requirements.txt
#
# Lower bounds are the versions this was developed and tested against; they are
# not upper-pinned so a fresh install picks up fixes.

# --- The agent: LLM calls through the HuggingFace router (OpenAI-compatible) ---
openai>=2.21.0
requests>=2.32.0
urllib3>=2.5.0

# --- Geometry and the physical checkers ---
numpy>=2.2.0
scipy>=1.17.0
python-fcl>=0.7.0          # swept-volume collision, ldr_collision_checker.py
Pillow>=12.1.0             # render contact sheets

# --- Retrieval: local embedding + reranking over the parts/sets corpora ---
# torch is the large one. A CPU-only build is fine and much smaller — see the
# README; the GPU only makes index building and search faster, not possible.
torch>=2.9.0
transformers>=4.57.0
huggingface-hub>=0.36.0
safetensors>=0.7.0

# --- The web app ---
fastapi>=0.131.0
uvicorn>=0.41.0
pydantic>=2.12.0

# --- Instruction booklets (font metrics for the page layout) ---
fonttools>=4.61.0

# --- Used by `run_agent.py --self-test` to sweep for undefined names ---
pyflakes>=3.4.0
